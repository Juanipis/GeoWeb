/*
document.addEventListener("DOMContentLoaded", function () { 
    var angulo = document.getElementById('agudo');
    var value = 0;
    var condicion = true

    function anguloAgudo(){
        angulo.setAttribute('transform', 'rotate(-'+valores() + ' 50 50)');
        
    }
    function valores() {
        if (value == 0) {
            
        }
        value = value + 0.1;
        return value;
    }


  setInterval(anguloAgudo, 10);
  }
);
*/